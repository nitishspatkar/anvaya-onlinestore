# Stitch — Editorial Heritage

- **`DESIGN.md`** — written spec (surfaces, typography, no-line rule, components).
- **`prototypes/`** — static HTML exports from Stitch (reference layouts):
  - `quick_access.html` — home / mobile shell (hero, chips, bottom nav, FAB).
  - `collection_desktop.html` — desktop nav + collection grid.
  - `vetiver_desktop.html` — source family / category editorial page.

The Next.js app implements these patterns in `app/` and `components/`.
